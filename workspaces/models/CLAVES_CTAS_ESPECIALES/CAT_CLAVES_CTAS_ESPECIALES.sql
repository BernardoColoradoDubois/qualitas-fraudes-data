SELECT 
  -- CUENTA: String -> STRING (mantiene como string)
  CUENTA,
  
  -- CVE_AGENTE: Integer -> INT64
  CVE_AGENTE,
  
  -- EJECUTIVA: String -> STRING (mantiene como string)
  EJECUTIVA

FROM `qlts-dev-mx-au-bro-verificacio.LAN_VERIFICACIONES.CLAVES_CTAS_ESPECIALES`;