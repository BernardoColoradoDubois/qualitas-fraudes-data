
# --- Appearance ---


#export ZSH_THEME="agnoster"  # or powerlevel10k if you install it

bindkey "^[[1;3C" forward-word    # Option + →
bindkey "^[[1;3D" backward-word   # Option + ←
export WORDCHARS='-_'


# --- Plugins ---
plugins=(

  git
  docker
  kubectl
  gcloud
  pip
  python
  poetry
  zsh-autosuggestions
  zsh-syntax-highlighting
)

# --- Source Oh My Zsh ---
#export ZSH="$HOME/.oh-my-zsh"
#source $ZSH/oh-my-zsh.sh

# --- Aliases ---
alias gs='git status'
alias gp='git pull'
alias dc='docker compose'
alias k='kubectl'
alias py='python3'
alias venv='python3 -m venv venv && source venv/bin/activate'
alias bqq='bq query --use_legacy_sql=false'
alias df='gcloud datafusion'
alias dp='gcloud dataproc'

# --- Options ---
bindkey "^[[3;5~" kill-word             # Option+Delete
bindkey "^[[1;5C" forward-word          # Ctrl+Right
bindkey "^[[1;5D" backward-word         # Ctrl+Left

export PATH="/opt/homebrew/bin:$PATH"
# Make ls use colors
export CLICOLOR=1
export LSCOLORS=GxFxCxDxBxegedabagaced
alias ls='ls -G'  # macOS color flag
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'

# Simple, colorful prompt
autoload -Uz vcs_info

precmd() { vcs_info }

setopt prompt_subst

PROMPT='%F{cyan}%n@%m%f:%F{green}%~%f %F{yellow}${vcs_info_msg_0_}%f
❯ '

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/saulcordova/Downloads/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/saulcordova/Downloads/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/saulcordova/Downloads/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/saulcordova/Downloads/google-cloud-sdk/completion.zsh.inc'; fi
